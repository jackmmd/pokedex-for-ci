#Nombre del workflow
name: My workflow

on:
  push: 
    branches: 
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Ejecutando primer paso
        run: |
          echo 'Hola Duberly Mondragón'
        
      - name: Dime la fecha
        run: date
      - run: ls -l
      - run: whoami
      - run: pwd
        




